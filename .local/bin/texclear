#!/bin/sh

[ -f "$1.tex" ] || { echo $1.tex not found && exit 1; }

del=""
for ext in aux log dvi bbl blg; do
	[ ! -f "$1.$ext" ] && continue
	rm "$1.$ext"
	del=$(printf "%s%s " "$del" "$1.$ext")
done
echo cleared $del
